services:
  - type: web
    name: jarvis-ai
    runtime: node
    buildCommand: npm install && npx tsc
    startCommand: node dist/cloud-server.js
    envVars:
      - key: GROQ_API_KEY
        sync: false
      - key: NODE_ENV
        value: production
      - key: PORT
        value: "3000"
